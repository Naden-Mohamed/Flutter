import 'package:flutter/material.dart';

class AppColors {
  static final yellow = Color(0xffFBD512); //ff for opacity we can change them
  //i have to add 0xff then the hex code of the color
  static final background = Color(0xff1A2947);
}
